class UserStatsManager { constructor() { this.moodUsage = {}; this.genreUsage = {}; this.tempoSum = 0; this.songCount = 0; } addMood(mood) { if (!this.moodUsage[mood]) { this.moodUsage[mood] = 0; } this.moodUsage[mood]++; } addGenre(genre) { if (!this.genreUsage[genre]) { this.genreUsage[genre] = 0; } this.genreUsage[genre]++; } addTempo(tempo) { this.tempoSum += tempo; this.songCount++; } getAverageTempo() { if (this.songCount === 0) return 0; return this.tempoSum / this.songCount; } resetStats() { this.moodUsage = {}; this.genreUsage = {}; this.tempoSum = 0; this.songCount = 0; } }